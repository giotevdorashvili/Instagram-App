import Logo from '../../assets/instagram-wordmark.svg';
import {LogoTypes} from './LogoTypes';

const InstagramWordLogo = (props: LogoTypes) => {
  return <Logo {...props} />;
};
export default InstagramWordLogo;
